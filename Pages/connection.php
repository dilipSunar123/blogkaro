<?php
    // Server name
    $server = "localhost";
    // User name
    $user = "root";
    // Password
    $password = "";
    // Database
    $database = "blogkaro";

    // mysql query to connect to the database
    $conn = mysqli_connect($server, $user, $password, $database);
?>